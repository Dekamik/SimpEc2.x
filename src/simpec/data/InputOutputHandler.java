package simpec.data;

public class InputOutputHandler {
	
	public InputOutputHandler() {
		
	}
	
	/**
	 * Saves a file to the harddrive.
	 * 
	 * @param path	A String that represents the path to the file to be loaded. 
	 * @return		True if the operation is successful
	 */
	public boolean saveData(String path) {
		return false;
	}
	
	/**
	 * Loads a file from the harddrive that contains a data class that extends Data.
	 * 
	 * @param path	A String that represents the path that the file should be saved to.
	 * @return		The data class that extends Data.
	 */
	public Data loadData(String path) {
		return null;
	}
}
